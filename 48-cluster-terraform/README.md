# Cluster local com config minima

- Istio-base
- Istiod
- external-secrets

## Instalação

- Download dos resources e inicialização do projeto:
    ```sh
    terraform init
    ```

- Instalando istio:
    ```sh
    terraform apply -auto-approve
    ```
